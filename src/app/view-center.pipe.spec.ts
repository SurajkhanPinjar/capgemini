import { ViewCenterPipe } from './view-center.pipe';

describe('ViewCenterPipe', () => {
  it('create an instance', () => {
    const pipe = new ViewCenterPipe();
    expect(pipe).toBeTruthy();
  });
});
